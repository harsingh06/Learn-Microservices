name: candidate-service

on:
  push:
    branches: [main]
    paths:
      - "src/CandidateService/**"
      - "tests/CandidateService.Tests/**"
      - ".github/workflows/candidate-service.yml"
      - ".github/workflows/service-pipeline.yml"
  pull_request:
    paths:
      - "src/CandidateService/**"
      - "tests/CandidateService.Tests/**"
      - ".github/workflows/candidate-service.yml"
      - ".github/workflows/service-pipeline.yml"

permissions:
  contents: read
  id-token: write

jobs:
  pipeline:
    uses: ./.github/workflows/service-pipeline.yml
    with:
      service-name: CandidateService
      image-name: candidate-service
      app-name: ats-candidate
      deploy: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    secrets: inherit
